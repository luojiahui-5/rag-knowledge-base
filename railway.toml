[build]
builder = "nixpacks"
buildCommand = "pip3 install -r backend/requirements.txt && npm install && npm run build"

[deploy]
startCommand = "cd backend && python3 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
